export const MATRIX_START_GRID_SIZE = 3;
export const MATRIX_MAX_GRID_SIZE = 6;
export const MATRIX_START_LIT_COUNT = 3;
export const MATRIX_MAX_LIT_COUNT = 12;

// Flat for v1 — how long the lit tiles stay visible before the grid clears
// and input opens.
export const MATRIX_STUDY_MS = 3000;

// How long the reveal (correct tiles green, wrong tile red) stays on screen
// before advancing to the next level or ending the run.
export const MATRIX_REVEAL_MS = 900;
